# Image-Classification-with-Pre-trained-ResNet-18
This app uses a pre-trained ResNet-18 model to classify uploaded images into one of 1,000 classes. It runs entirely on CPU and shows the top-5 predictions with confidence scores and a bar chart.
